import React from 'react';
import { Button } from '../components';

const LoginWithTwitter = () => {
  return (
    <div>
      <Button isFullWidth>Login with Twitter</Button>
    </div>
  );
};

export default LoginWithTwitter;
