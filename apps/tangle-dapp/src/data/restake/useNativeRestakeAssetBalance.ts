/**
 * Hook to get native asset balance for restaking.
 * Returns null as native restaking via staking is deprecated in v2.
 */
const useNativeRestakeAssetBalance = () => {
  return null;
};

export default useNativeRestakeAssetBalance;
