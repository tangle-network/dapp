export type MetricReturnType = {
  value1?: number;
  value2?: number;
};

export type Validator = {
  address: string;
  identity: string;
  selfStaked: string;
  effectiveAmountStaked: string;
  delegations: string;
  minimumStake: string;
};
