export default function SandboxUiStyles() {
  // sandbox-ui is loaded once at the app root. Keep this component as a
  // compatibility shim for older blueprint modules that still render it.
  return null;
}
