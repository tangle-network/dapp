{
  "name": "tangle-cloud",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/tangle-cloud/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project tangle-cloud --web",
  "targets": {
    "build": {
      "dependsOn": ["typecheck"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/tangle-cloud/tsconfig.app.json"
      }
    }
  }
}
