import { FC } from '../../../../node_modules/react';
export type ListStatusProps = {
    emoji?: string;
    className?: string;
    title: string;
    description?: string;
};
export declare const ListStatus: FC<ListStatusProps>;
