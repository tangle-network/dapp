/**
 * The hook use the read the UI Context configurations (ex: `theme`, etc...)
 * @returns `UIContext` contains all configures for the UI Context
 */
export declare function useUIContext(): import('../provider/types').IUIContext;
