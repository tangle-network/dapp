query MetaData {
    _metadata {
        targetHeight
        lastProcessedHeight
    }
}
query LastBlock{
    blocks(first: 1, orderBy:[NUMBER_DESC]) {
        nodes {
            timestamp
            number
        }
    }
}
