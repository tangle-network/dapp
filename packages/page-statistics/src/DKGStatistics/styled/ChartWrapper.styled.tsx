import { above } from '@webb-dapp/ui-components/utils/responsive-utils';
import styled from 'styled-components';

import { CardWrapper } from './shared/CardWrapper.styled';

export const ChartWrapper = styled(CardWrapper)`
  flex-grow: 1;
  max-width: 550px;
`;
