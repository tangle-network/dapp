import React from 'react';
import styled from 'styled-components';

const DepositWrapper = styled.div``;
type DepositProps = {};

export const Deposit: React.FC<DepositProps> = ({}) => {
  return <DepositWrapper>Deposit</DepositWrapper>;
};
