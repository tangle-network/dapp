import React from 'react';
import styled from 'styled-components';

const MixerTabsWrapper = styled.div``;
type MixerTabsProps = {};

export const MixerTabs: React.FC<MixerTabsProps> = ({}) => {
  return <MixerTabsWrapper>MixerTabs</MixerTabsWrapper>;
};
