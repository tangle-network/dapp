const WebbIcon = require('./coins-icon/webb-icon.svg') as string;

export const TOKEN_COLOR: Map<string, string> = new Map([['SYSTEM', '#173DC9']]);

export const TOKEN_IMAGES: Map<string, string> = new Map([['WEBB', WebbIcon]]);

export const TOKEN_FULLNAMES: Map<string, string> = new Map([['WEBB', 'WebbToken']]);
